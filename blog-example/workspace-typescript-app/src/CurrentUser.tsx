import * as React from "react";

export default function CurrentUser() {
  // TODO:
  //
  //   - Frage den AuthContext mit dem useAuthContext-Hook ab
  //     und prüfe, ob ein Benutzer eingeloggt ist
  //
  //   - Passe den Rückgabetyp an und gib einen Button statt "null" zurück:
  //   - Wenn ein Benutzer eingeloggt ist:
  //      Liefer einen "Logout"-button zurück, der beim Klick den Benutzer wieder ausloggt
  //   - Wenn KEIN Benutzer eingeloggt ist:
  //      Liefer einen "Login"-Button zurück, der beim Klick einen Benutzer einloggt
  //       - verwende irgendeinen beliebigen Namen als neuen 'username'
  //       - ein Login-Formular bauen wir später
  //
  //   Hinweis:
  //    - Diese Komponente ist bereits in AppHeader.tsx eingebunden, wenn sie korrekt
  //      funktioniert, sollten oben rechts in der Anwendung deine Button erscheinen!

  return null;
}
